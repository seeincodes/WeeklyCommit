{
  "name": "@wc/ui-components",
  "version": "0.1.0",
  "private": true,
  "description": "Flowbite React wrappers and shared UI primitives for the Weekly Commit module.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo '@wc/ui-components: no lint (placeholder lib; component lint lands when components do)'",
    "typecheck": "echo '@wc/ui-components: no typecheck (placeholder; tsconfig lands when components do)'",
    "test": "echo '@wc/ui-components: no tests (placeholder; tests land when components do)'"
  }
}
