/**
 * Route shell for /weekly-commit/history. The historical-weeks list ships
 * later in group 11; this shell exists so the routing layer has something
 * to render.
 */
export function HistoryPage() {
  return <div data-testid="history-page" />;
}
