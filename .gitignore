# ------------------------------------------------------------
# Tooling caches
# ------------------------------------------------------------
.claude/

# ------------------------------------------------------------
# Environment & secrets
# ------------------------------------------------------------
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
*.pem
*.p12
# Targeted whitelist for the E2E test keypair. Both files have RFC-grade docs
# in the same directory explaining why they're not production secrets (zero
# authority outside the `e2e` Spring profile, which never runs in production).
# Every other .pem in the repo stays ignored by default.
!apps/weekly-commit-service/src/main/resources/e2e-keys/*.pem
!apps/weekly-commit-ui/cypress/support/auth/keys/*.pem
.terraform.tfstate
.terraform.tfstate.backup
.terraform/
*.tfvars
!terraform.tfvars.example

# ------------------------------------------------------------
# Node / Yarn / Vite / Frontend
# ------------------------------------------------------------
node_modules/
.yarn/cache
.yarn/install-state.gz
.yarn/build-state.yml
.pnp.*
.cache/
.nx/cache
.nx/workspace-data
dist/
build/
out/
.parcel-cache/
.vite/
.turbo/
*.tsbuildinfo
coverage/
.vitest-cache/
.playwright/
test-results/
playwright-report/
cypress/videos/
cypress/screenshots/
cypress/downloads/
.sentryclirc

# ------------------------------------------------------------
# Java / Spring Boot / Maven / Gradle
# ------------------------------------------------------------
target/
!src/**/target/
build/
!src/**/build/
.gradle/
gradle-app.setting
.gradletasknamecache
*.class
*.jar
!mvnw.jar
!gradle-wrapper.jar
*.war
*.nar
*.ear
*.log
hs_err_pid*
replay_pid*
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar
dependency-reduced-pom.xml

# JaCoCo / PITest / SpotBugs / Checkstyle reports
jacoco.exec
*.exec
site/
pit-reports/
spotbugsXml.xml

# ------------------------------------------------------------
# IDEs / editors
# ------------------------------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json
.settings/
.project
.classpath
.factorypath
*.swp
*.swo
*~
.DS_Store

# ------------------------------------------------------------
# OS / misc
# ------------------------------------------------------------
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.AppleDouble
.LSOverride

# ------------------------------------------------------------
# Logs
# ------------------------------------------------------------
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ------------------------------------------------------------
# Testcontainers / Docker scratch
# ------------------------------------------------------------
.testcontainers/

# ------------------------------------------------------------
# Generated OpenAPI clients
# ------------------------------------------------------------
# libs/contracts/generated/types.d.ts IS committed -- the openapi.yaml +
# openapi-typescript pair is treated like Spotless: regenerate via
# `yarn workspace @wc/contracts generate:ts` after changing the spec; CI
# fails if drift is detected (see scripts/verify:ts).
